{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Modification
{% endblock %}

{% block body %}
    <div>
        {{ form_start(animalForm) }}

        {{ form_widget(animalForm) }}

        <button>Modifier !</button>

        {{ form_end(animalForm) }}
    </div>

{% endblock %}