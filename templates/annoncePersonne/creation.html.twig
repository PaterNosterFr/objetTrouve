{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Objets
{% endblock %}

{% block body %}
    <div>
        {{ form_start(personneForm) }}

        {{ form_widget(personneForm) }}

        <button>Créer</button>

        {{ form_end(personneForm) }}
    </div>

{% endblock %}