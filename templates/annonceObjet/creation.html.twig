{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Objets
{% endblock %}

{% block body %}
    <div>
        {{ form_start(objetForm) }}

        {{ form_widget(objetForm) }}

        <button>Créer</button>

        {{ form_end(objetForm) }}
    </div>

{% endblock %}