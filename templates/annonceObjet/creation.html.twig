{% extends 'base.html.twig' %}

{% block title %}
    Objet perdu ? | {{ parent() }}
{% endblock %}

{% block body %}
    <div>
        {{ form_start(objetForm) }}

        {{ form_widget(objetForm) }}

        <button>Créez !</button>

        {{ form_end(objetForm) }}
    </div>

{% endblock %}