{% extends 'base.html.twig' %}

{% block title %}
{{objet.name}} | {{ parent() }}
{% endblock %}

{% block body %}
    <h2>Titre : {{ objet.name}}</h2>
    <br>
    <div>----------</div>
    <div><button><a href="{{ path('annonceObjet_edition', {'id' : objet.id}) }}">Editer</a></button>
        /
        <button onclick="function suppression(){{ objet.id }}" >Supprimer</button>
    </div>
    <div>----------</div>
    <br>
    <div>Date : {{ objet.date|date('d-m-Y')}}</div>
    <div>Adresse : {{ objet.lieu }}</div>
    <div>Photo : <img src="{{ asset('img/objet/' ~ objet.photo) }}" alt="{{ objet.name }}" class="miniature"></div>

{% endblock %}
